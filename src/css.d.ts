// Declares plain CSS side-effect imports (e.g. `import "./globals.css"`).
// Next.js only ships a `*.module.css` declaration, so editors with
// `noUncheckedSideEffectImports` enabled flag bare CSS imports without this.
declare module "*.css";
